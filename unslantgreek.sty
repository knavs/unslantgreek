\ProvidesPackage{unslantgreek}[2024/11/03 Upright Greek Letters with Improved Centering]

% Define save box and length for precise character positioning
\newsavebox{\foobox}
\newlength{\foodim}

% Define the \slantbox command to unslant symbols by a specified factor.
%  - #1 is the optional slant factor (default is -.16 in the \unslant command).
%  - #2 is the content (typically a Greek letter) to be displayed.
\newcommand{\slantbox}[2][0]{\mbox{%
        \sbox{\foobox}{#2}%          % Store the content in \foobox
        \foodim=#1\wd\foobox         % Calculate shift distance based on slant factor
        \hskip \wd\foobox            % Horizontal space equal to box width
        \hskip -0.5\foodim           % Adjust left by half of calculated shift
        \pdfsave
        \pdfsetmatrix{1 0 #1 1}%     % Apply slant transformation
        \llap{\usebox{\foobox}}%     % Draw the content with transformation applied
        \pdfrestore
        \hskip 0.5\foodim            % Adjust back to center content
}}

% Define \unslant with a default slant factor of -.16 (adjustable if needed)
\newcommand\unslant[2][-.25]{\slantbox[#1]{$#2$}}

% Original Letters as \slalpha, \slbeta, etc.:
%  - The original (slanted) versions of Greek letters are saved under new names
%    prefixed with "sl" (e.g., \slalpha, \slbeta).
%  - This makes the original slanted versions accessible in the document if needed.

% Redefinitions for Upright Letters:
%  - Each Greek letter now defaults to the unslanted version, achieved by calling
%    \unslant on the original symbol (saved under the \sl prefix).
%  - This means the user will see upright versions by default when using standard
%    Greek letter commands, but can still access the slanted versions with the \sl prefix.

% Lowercase Greek letters
\let\slalpha=\alpha           \def\alpha{\unslant{\slalpha}}
\let\slbeta=\beta             \def\beta{\unslant{\slbeta}}
\let\slgamma=\gamma           \def\gamma{\unslant{\slgamma}}
\let\sldelta=\delta           \def\delta{\unslant[-.2]{\sldelta}}
\let\slepsilon=\epsilon       \def\epsilon{\unslant[-.1]{\slepsilon}}
\let\slvarepsilon=\varepsilon \def\varepsilon{\unslant{\slvarepsilon}}
\let\slzeta=\zeta             \def\zeta{\unslant[-.15]{\slzeta}}
\let\sleta=\eta               \def\eta{\unslant{\sleta}}
\let\sltheta=\theta           \def\theta{\unslant{\sltheta}}
\let\slvartheta=\vartheta     \def\vartheta{\unslant{\slvartheta}}
\let\sliota=\iota             \def\iota{\unslant{\sliota}}
\let\slkappa=\kappa           \def\kappa{\unslant{\slkappa}}
\let\sllambda=\lambda         \def\lambda{\unslant{\sllambda}}
\let\slmu=\mu                 \def\mu{\unslant{\slmu}}
\let\slnu=\nu                 \def\nu{\unslant{\slnu}}
\let\slxi=\xi                 \def\xi{\unslant[-.15]{\slxi}}
\let\slpi=\pi                 \def\pi{\unslant{\slpi}}
\let\slvarpi=\varpi           \def\varpi{\unslant{\slvarpi}}
\let\slrho=\rho               \def\rho{\unslant{\slrho}}
\let\slvarrho=\varrho         \def\varrho{\unslant{\slvarrho}}
\let\slsigma=\sigma           \def\sigma{\unslant{\slsigma}}
\let\slvarsigma=\varsigma     \def\varsigma{\unslant{\slvarsigma}}
\let\sltau=\tau               \def\tau{\unslant{\sltau}}
\let\slupsilon=\upsilon       \def\upsilon{\unslant{\slupsilon}}
\let\slphi=\phi               \def\phi{\unslant{\slphi}}
\let\slvarphi=\varphi         \def\varphi{\unslant{\slvarphi}}
\let\slchi=\chi               \def\chi{\unslant{\slchi}}
\let\slpsi=\psi               \def\psi{\unslant{\slpsi}}
\let\slomega=\omega           \def\omega{\unslant{\slomega}}

% Uppercase Greek letters already usnlanted nothin to do


\endinput